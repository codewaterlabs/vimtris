let tickDuration = 0.5;

let tileCols = 12;

let tileRows = 26;

let blinkTime = 0.7;

let dropAnimDuration = 0.8;

let dropDownBeforeTouchDown = 0.15;

let dropDownBeforeTick = 0.5;

let pointLight = Data.Vec3.make(0.0, -0.4, 2.0);

let camera = Data.Vec3.make(0.0, 0.4, 4.0);
